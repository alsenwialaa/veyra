# Traceability evidence policy

The two ledgers are copied from the canonical Veyra engineering skill for proposal v4.1: all 35 anchor rows and all 64 Definition of Done rows are present.

Every formal state remains `Not assessed`. The engineering candidate now contains connected implementation and deterministic test evidence for bounded slices, and those links are recorded on the relevant rows. No row yet has the complete live integration, recovery, compatibility, documentation, and named acceptance-owner evidence required to promote it to Implemented, Tested, or Accepted. Design presence or a partial local test is never enough on its own.

As vertical slices are built, update a row only with attributable file/commit, test report/environment, documentation, and named acceptance evidence. `Failed` and `Not assessed` block release. `Not applicable` requires a written proposal-compatible rationale and cannot remove a Production Core requirement.

Current summary: 0/35 anchors accepted; 0/64 DoD items accepted; release verdict NOT READY.
